import { Meta, Story, Canvas, Source } from "@storybook/addon-docs";

<Meta title="Docs/Components/Selection" />

# Selection

In Comet Admin, a `Selection` can be used to manage which item a user has currently selected.
It's most commonly used for keeping track of selections when combining a table and a form.

## Basic Example

<Story id="stories-components-selection-selection--basic-selection" />

tba.

-   Selection
-   useSelection
-   Selected
-   SelectionApi
-   SelectionRoute
-   useSelectionRoute
