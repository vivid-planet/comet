import { Meta, Story, Canvas } from "@storybook/addon-docs";

<Meta title="Docs/Components/Table/FilterBar/FilterBar" />

# Filterbar

The Filterbar Component helps you create a horizontal orientated list of PopoverFields which can include Final Form Fields.
The intended use case for the FilterBar is to act as a filter on top of a table.
**The Filterbar only works inside of a Final Form.**

The Filterbar consists of four different components:

-   FilterBar
-   FilterBarPopoverFilter
-   FilterBarMoreFilters
-   FilterBarActiveFilterBadge

The FilterBarActiveFilterBadge shows the current count of set values of every FilterBarPopoverFilter form. This component as well as the calculation can be overridden.

## Examples

<Canvas>
    <Story id="stories-components-table-filterbar-filterbar-default--default" />
</Canvas>

### With FilterBarMoreFilters

<Canvas>
    <Story id="stories-components-table-filterbar-filterbar-with-filterbar-more-filters--with-filterbar-more-filters" />
</Canvas>

### Multiple different Fields in one FilterBarPopoverFilter

<Canvas>
    <Story id="stories-components-table-filterbar-filterbar-multiple-fields--multiple-fields" />
</Canvas>
