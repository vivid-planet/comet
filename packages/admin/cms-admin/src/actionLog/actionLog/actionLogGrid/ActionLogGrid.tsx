import { gql, useQuery } from "@apollo/client";
import {
    dataGridDateTimeColumn,
    DataGridToolbar,
    GridCellContent,
    type GridColDef,
    GridFilterButton,
    MainContent,
    muiGridFilterToGql,
    muiGridSortToGql,
    ToolbarItem,
    Tooltip,
    useBufferedRowCount,
    useDataGridRemote,
    usePersistentColumnState,
} from "@comet/admin";
import { Time, View } from "@comet/admin-icons";
import { IconButton } from "@mui/material";
import { DataGrid } from "@mui/x-data-grid";
import { useMemo, useState } from "react";
import { FormattedMessage, useIntl } from "react-intl";

import { type ContentScope, useContentScope } from "../../../contentScope/Provider";
import { ActionChip } from "../../components/actionChip/ActionChip";
import { UserCell } from "../../components/userCell/UserCell";
import { ActionLogEntryDialog } from "../actionLogEntryDialog/ActionLogEntryDialog";
import { ActionLogShowVersionDialog } from "../actionLogShowVersionDialog/ActionLogShowVersionDialog";
import { actionLogGridFragment } from "./ActionLogGrid.gql";
import type { GQLActionLogGridFragment } from "./ActionLogGrid.gql.generated";

/**
 * Keys of `TQuery` whose value is a `PaginatedActionLogs` — i.e. the top-level
 * `<entity>ActionLogs` queries generated by `ActionLogsModule.forFeature()`.
 * Falls back to `string` when no generic is supplied.
 */
export type ActionLogQueryName<TQuery> = string extends keyof TQuery
    ? string
    : {
          [K in keyof TQuery]: NonNullable<TQuery[K]> extends { __typename?: "PaginatedActionLogs" } ? K : never;
      }[keyof TQuery] &
          string;

type ActionLogGridProps<TQuery> = {
    /**
     * Name of the top-level entity-scoped query field, e.g. `newsActionLogs`.
     *
     * Pass your app's `GQLQuery` as the generic to constrain this to a real action log query name.
     */
    queryName: ActionLogQueryName<TQuery>;
};

const displayNameFields = ["name", "title", "label", "slug", "description"] as const;

function extractDisplayName(snapshot: Record<string, unknown> | null | undefined): string | undefined {
    if (!snapshot) {
        return undefined;
    }
    for (const field of displayNameFields) {
        const value = snapshot[field];
        if (typeof value === "string" && value.length > 0) {
            return value;
        }
    }
    return undefined;
}

function ActionLogGridToolbar() {
    return (
        <DataGridToolbar>
            <ToolbarItem>
                <GridFilterButton />
            </ToolbarItem>
        </DataGridToolbar>
    );
}

type EntityActionLogsQueryResult = {
    [key: string]: { nodes: GQLActionLogGridFragment[]; totalCount: number };
};

export function buildEntityActionLogsQuery(queryName: string) {
    return gql`
        query ${queryName[0].toUpperCase() + queryName.slice(1)}($scope: JSONObject!, $offset: Int!, $limit: Int!, $filter: ActionLogFilter, $sort: [ActionLogSort!]) {
            ${queryName}(scope: $scope, offset: $offset, limit: $limit, filter: $filter, sort: $sort) {
                nodes {
                    ...ActionLogGrid
                }
                totalCount
            }
        }
        ${actionLogGridFragment}
    `;
}

export function ActionLogGrid<TQuery = Record<string, unknown>>({ queryName }: ActionLogGridProps<TQuery>) {
    const intl = useIntl();
    const { scope } = useContentScope();
    const [selectedRow, setSelectedRow] = useState<GQLActionLogGridFragment | null>(null);
    const [openEntityId, setOpenEntityId] = useState<string | null>(null);

    const actionLogsQuery = useMemo(() => buildEntityActionLogsQuery(queryName), [queryName]);

    const dataGridProps = {
        ...useDataGridRemote({ initialSort: [{ field: "createdAt", sort: "desc" }] }),
        ...usePersistentColumnState(`ActionLogGrid-${queryName}`),
    };

    const columns = useMemo<GridColDef<GQLActionLogGridFragment>[]>(
        () => [
            {
                ...dataGridDateTimeColumn,
                field: "createdAt",
                headerName: intl.formatMessage({ id: "comet.actionLog.entity.columns.createdAt", defaultMessage: "Date / Time" }),
                width: 200,
            },
            {
                field: "action",
                headerName: intl.formatMessage({ id: "comet.actionLog.entity.columns.action", defaultMessage: "Action" }),
                type: "singleSelect",
                sortable: false,
                width: 150,
                valueOptions: [
                    { value: "Created", label: intl.formatMessage({ id: "comet.actionLog.entity.action.created", defaultMessage: "Created" }) },
                    { value: "Updated", label: intl.formatMessage({ id: "comet.actionLog.entity.action.updated", defaultMessage: "Updated" }) },
                    { value: "Deleted", label: intl.formatMessage({ id: "comet.actionLog.entity.action.deleted", defaultMessage: "Deleted" }) },
                ],
                renderCell: ({ value }) => <ActionChip actionValue={value} label={value} />,
            },
            {
                field: "entityId",
                headerName: intl.formatMessage({ id: "comet.actionLog.entity.columns.entity", defaultMessage: "Entity" }),
                minWidth: 280,
                flex: 1,
                sortable: false,
                filterable: false,
                renderCell: ({ row }) => {
                    const displayName =
                        extractDisplayName(row.snapshot as Record<string, unknown> | null | undefined) ??
                        extractDisplayName(row.previousVersion?.snapshot as Record<string, unknown> | null | undefined);
                    return <GridCellContent primaryText={displayName ?? row.entityId} secondaryText={displayName ? row.entityId : undefined} />;
                },
            },
            {
                field: "user",
                headerName: intl.formatMessage({ id: "comet.actionLog.entity.columns.user", defaultMessage: "User" }),
                minWidth: 200,
                flex: 1,
                sortable: false,
                filterable: false,
                renderCell: ({ row }) => <UserCell id={row.user.id} name={row.user.name ?? undefined} />,
            },
            {
                field: "actions",
                type: "actions",
                headerName: "",
                width: 100,
                sortable: false,
                filterable: false,
                renderCell: ({ row }) => (
                    <>
                        <Tooltip title={<FormattedMessage id="comet.actionLog.entity.actions.showVersion" defaultMessage="Show version" />}>
                            <IconButton color="primary" onClick={() => setSelectedRow(row)}>
                                <View />
                            </IconButton>
                        </Tooltip>
                        <Tooltip
                            title={
                                <FormattedMessage
                                    id="comet.actionLog.entity.actions.showEntityActionLog"
                                    defaultMessage="Show action log for this entity"
                                />
                            }
                        >
                            <IconButton onClick={() => setOpenEntityId(row.entityId)}>
                                <Time />
                            </IconButton>
                        </Tooltip>
                    </>
                ),
            },
        ],
        [intl],
    );

    const { filter: gqlFilter } = muiGridFilterToGql(columns, dataGridProps.filterModel);

    const { data, loading, error } = useQuery<EntityActionLogsQueryResult>(actionLogsQuery, {
        variables: {
            scope: scope as ContentScope,
            offset: dataGridProps.paginationModel.page * dataGridProps.paginationModel.pageSize,
            limit: dataGridProps.paginationModel.pageSize,
            filter: gqlFilter,
            sort: muiGridSortToGql(dataGridProps.sortModel),
        },
    });

    const result = data?.[queryName];
    const rowCount = useBufferedRowCount(result?.totalCount);

    if (error) {
        throw error;
    }

    return (
        <MainContent fullHeight>
            <DataGrid
                {...dataGridProps}
                columns={columns}
                rows={result?.nodes ?? []}
                rowCount={rowCount}
                loading={loading}
                disableRowSelectionOnClick
                onRowClick={({ row }) => setSelectedRow(row)}
                slots={{ toolbar: ActionLogGridToolbar }}
                showToolbar
            />
            <ActionLogShowVersionDialog queryName={queryName} row={selectedRow} open={selectedRow !== null} onClose={() => setSelectedRow(null)} />
            {openEntityId !== null && (
                <ActionLogEntryDialog queryName={queryName} entityId={openEntityId} open onClose={() => setOpenEntityId(null)} />
            )}
        </MainContent>
    );
}

export { actionLogGridFragment } from "./ActionLogGrid.gql";
