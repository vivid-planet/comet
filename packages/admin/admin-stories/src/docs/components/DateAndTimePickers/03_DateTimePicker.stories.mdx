import { Meta, Canvas, Story, Source } from "@storybook/addon-docs";
import DatePickerLocalisation from "./common/DatePickerLocalization.mdx";

<Meta title="Docs/Components/Date & Time Pickers/Date-Time Picker" />

# Date-Time Picker

To use the `DateTimePicker` and `FinalFormDateTimePicker` components, install the `@comet/admin-date-time` package.

## Value type

The `value` prop and the value returned by `onChange` are of type `Date`.

<DatePickerLocalisation />

## Basic usage

<Canvas>
    <Story id="stories-components-date-time-pickers-date-time-picker--basic" />
</Canvas>

## Usage inside a final-form

<Canvas>
    <Story id="stories-components-date-time-pickers-date-time-picker--final-form" />
</Canvas>
