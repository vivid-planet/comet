import styled from "styled-components";

export const ExceptionContainerDetails = styled.details`
    white-space: pre-wrap;
`;

export const ExceptionContainerSummary = styled.summary`
    cursor: pointer;
    outline: none;
`;
