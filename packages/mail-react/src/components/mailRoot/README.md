# MjmlMailRoot

Every email template repeats the same boilerplate document skeleton — head, body, breakpoint, attribute defaults, theme and config wiring. `MjmlMailRoot` is a single root component that assembles all of it from the resolved theme and lets templates start at section level.
