import { ToolbarTitleItemThemeProps } from "@comet/admin";

export const cometAdminToolbarTitleItemProps = (): ToolbarTitleItemThemeProps => ({
    typographyProps: {
        variant: "h4",
    },
});
