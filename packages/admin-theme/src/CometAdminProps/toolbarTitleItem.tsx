import { ToolbarTitleItemThemeProps } from "@comet/admin";

export const getCometAdminToolbarTitleItemProps = (): ToolbarTitleItemThemeProps => ({
    typographyProps: {
        variant: "h4",
    },
});
