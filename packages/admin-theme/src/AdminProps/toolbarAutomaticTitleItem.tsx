import { ToolbarAutomaticTitleItemThemeProps } from "@comet/admin";

export const getToolbarAutomaticTitleItemProps = (): ToolbarAutomaticTitleItemThemeProps => ({
    typographyProps: {
        variant: "h4",
    },
});
