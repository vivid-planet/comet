import { MasterLayoutThemeProps } from "@comet/admin";
import { Hamburger } from "@comet/admin-icons";

export const getMasterLayoutProps = (): MasterLayoutThemeProps => ({
    menuIcon: Hamburger,
});
