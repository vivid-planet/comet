# @comet/skills

Reusable AI coding agent skills for Comet projects.

This package publishes the skill folders from this repository's `skills/` directory so they can be versioned and released together with the other `@comet/*` packages.
