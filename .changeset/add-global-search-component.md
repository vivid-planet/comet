---
"@comet/cms-admin": minor
---

Add reusable `GlobalSearch` component that queries the `fullTextSearch` GraphQL endpoint and renders a search input with results dropdown in the header bar. Import it from `@comet/cms-admin` and place it in the `Header` component to enable full-text search across all entities.
