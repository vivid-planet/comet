---
"@comet/brevo-admin": patch
"@comet/cms-admin": patch
---

Replace the deprecated `@comet/admin-date-time` pickers with the new MUI X-based date/time components from `@comet/admin`

This updates the appearance of the affected date/time inputs (the DAM file license duration in `@comet/cms-admin` and the email campaign schedule in `@comet/brevo-admin`) to the new components.
