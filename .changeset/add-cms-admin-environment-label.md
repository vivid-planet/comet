---
"@comet/cms-admin": minor
---

Add optional `environmentLabel` support to `CometConfigProvider` to indicate non-production environments in the admin UI.

When configured, the label is appended to the browser tab title and shown in the About dialog.
