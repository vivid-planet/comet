---
"@comet/cms-api": minor
---

Add `FullTextSearchResolver` to the generated GraphQL schema

The `fullTextSearch` query and its `PaginatedEntityInfo` type are now included in `schema.gql`, so the query is available in the published API schema.
