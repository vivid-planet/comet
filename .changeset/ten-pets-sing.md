---
"@comet/cms-admin": minor
---

Add multiple selection to `RedirectsGrid`

Multiple redirects can now be selected via checkboxes. Selected redirects can be deleted at once using the bulk delete action in the toolbar.
