---
"@comet/admin": minor
---

Add DataGrid helper `useDataGridLocationState` that can be used like `useDataGridRemote` to store state as location params but for client side (non paginated) filter/sort
