---
"@comet/eslint-config": major
---

Add `curly` ESLint rule to enforce consistent use of braces for all control statements. This rule requires braces around the body of all control statements (if, else, for, while, etc.) to improve code readability and reduce diff size when adding statements.
