---
"@comet/cms-api": patch
"@comet/cms-admin": patch
---

Add a dedicated `pageTreeDeleteNode` permission for deleting page tree nodes and hide the delete action in Admin when the user lacks that permission.
