---
"@comet/admin-generator": minor
---

Add `initialPageSize` option to `GridConfig` for the Admin Generator. When set, the value is passed as `pageSize` to `useDataGridRemote` in the generated DataGrid code.
