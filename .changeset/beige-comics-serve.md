---
"@comet/cms-admin": minor
"@comet/admin": minor
---

Redesign `DamTable` to match the Comet DXP design

Remove `Toolbar` from `DamTable` page.
Remove `FolderHead` and use `DataGridToolbar` instead.
