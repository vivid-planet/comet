---
"@comet/admin": major
---

Add `react-hook-form` as a required peer dependency (>=7.35.0). Install it alongside `@comet/admin`:

```
npm install react-hook-form
```

New experimental react-hook-form components: `RHFForm`, `RHFTextField`, `RHFNumberField`.
