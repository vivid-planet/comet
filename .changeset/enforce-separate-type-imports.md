---
"@comet/eslint-config": major
---

Enforce separate type imports instead of inline type imports

Change `fixStyle` to `"separate-type-imports"` to disallow inline type imports (`import { type X }`) in favor of separate type imports (`import type { X }`).
