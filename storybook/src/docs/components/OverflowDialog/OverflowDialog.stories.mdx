import { Meta, Story, Canvas } from "@storybook/addon-docs";

<Meta title="Docs/Components/OverflowDialog" />

# OverflowDialog

Use `OverflowDialog` to wrap content that may be too large to fit its container. <br />
If the content is too large, it will be truncated. When clicked, the entire content will be displayed in a dialog.

```tsx
<OverflowDialog>
    {/* Lots of content ... */}
</OverflowDialog>
```

## Use Cases

A common use-case would be to show rich-text content in a `DataGrid`. <br />
If used outside a `DataGrid`, the height must be limited by a parent container, e.g.:

```tsx
<Box sx={{ height: 100 }}>
    <OverflowDialog>
        {/* Lots of content ... */}
    </OverflowDialog>
</Box>
```

## Basic Usage

<Canvas>
    <Story id="stories-components-overflowdialog--basic-usage" />
</Canvas>

## Usage inside `DataGrid`

<Canvas>
    <Story id="stories-components-overflowdialog--in-datagrid" />
</Canvas>
