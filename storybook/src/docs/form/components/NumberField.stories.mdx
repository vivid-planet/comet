import { Meta, Canvas, Story, Source } from "@storybook/addon-docs";

<Meta title="Docs/Form/Components/NumberField" />

# Number Field

The NumberField is used to input a number (with thousand-separators and decimal symbols in the current locale) while storing it as type `number` in the form values.

## Basic usage

<Canvas>
    <Story id="stories-form-components--numberfield" />
</Canvas>

## Usage with decimals

The decimals prop is used to define, how many decimal digits the value should have.

<Canvas>
    <Story id="stories-form-components--withdecimals" />
</Canvas>

## Usage as Currency Field

<Canvas>
    <Story id="stories-form-components--currency" />
</Canvas>
