import { Meta, Canvas, Story, Source } from "@storybook/addon-docs";
import DatePickerLocalisation from "./common/DatePickerLocalization.mdx";

<Meta title="Docs/Form/Components/Date & Time Pickers/Date Picker" />

# Date Picker

To use the `DatePicker` and `FinalFormDatePicker` components, install the `@comet/admin-date-time` package.

## Value type

The `value` prop and the value returned by `onChange` are a string in the format `YYYY-MM-DD`.

<DatePickerLocalisation />

## Basic usage

<Canvas>
    <Story id="stories-form-components-date-time-pickers-date-picker--basic" />
</Canvas>

## Usage inside a Final Form

<Canvas>
    <Story id="stories-form-components-date-time-pickers-date-picker--final-form" />
</Canvas>
