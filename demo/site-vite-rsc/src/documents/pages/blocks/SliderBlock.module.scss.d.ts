export type Styles = {
    mediaWrapper: string;
    slider: string;
    sliderItemBlockRoot: string;
    swiper: string;
    swiperContainer: string;
};

export type ClassNames = keyof Styles;

declare const styles: Styles;

export default styles;
