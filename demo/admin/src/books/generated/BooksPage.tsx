// This file has been generated by comet admin-generator.
// You may choose to use this file as scaffold by moving this file out of generated folder and removing this comment.

import { Stack, StackPage, StackSwitch } from "@comet/admin";
import * as React from "react";
import { useIntl } from "react-intl";

import { BookForm } from "./BookForm";
import { BooksGrid } from "./BooksGrid";

export function BooksPage(): React.ReactElement {
    const intl = useIntl();
    return (
        <Stack topLevelTitle={intl.formatMessage({ id: "books.books", defaultMessage: "Books" })}>
            <StackSwitch>
                <StackPage name="grid">
                    <BooksGrid />
                </StackPage>
                <StackPage name="edit" title={intl.formatMessage({ id: "books.editBook", defaultMessage: "Edit Book" })}>
                    {(selectedId) => <BookForm id={selectedId} />}
                </StackPage>
                <StackPage name="add" title={intl.formatMessage({ id: "books.addBook", defaultMessage: "Add Book" })}>
                    <BookForm />
                </StackPage>
            </StackSwitch>
        </Stack>
    );
}
