// This file has been generated by comet admin-generator.
// You may choose to use this file as scaffold by moving this file out of generated folder and removing this comment.
import { FormattedMessage } from "react-intl";
import { useApolloClient } from "@apollo/client";
import { useQuery } from "@apollo/client";
import { filterByFragment } from "@comet/admin";
import { FinalForm } from "@comet/admin";
import { FinalFormSubmitEvent } from "@comet/admin";
import { Loading } from "@comet/admin";
import { TextField } from "@comet/admin";
import { useFormApiRef } from "@comet/admin";
import { useStackSwitchApi } from "@comet/admin";
import { queryUpdatedAt } from "@comet/cms-admin";
import { resolveHasSaveConflict } from "@comet/cms-admin";
import { useFormSaveConflict } from "@comet/cms-admin";
import { FormApi } from "final-form";
import { useMemo } from "react";
import { productTagFormFragment } from "./ProductTagForm.gql";
import { GQLProductTagFormFragment } from "./ProductTagForm.gql.generated";
import { productTagQuery } from "./ProductTagForm.gql";
import { GQLProductTagQuery } from "./ProductTagForm.gql.generated";
import { GQLProductTagQueryVariables } from "./ProductTagForm.gql.generated";
import { createProductTagMutation } from "./ProductTagForm.gql";
import { GQLCreateProductTagMutation } from "./ProductTagForm.gql.generated";
import { GQLCreateProductTagMutationVariables } from "./ProductTagForm.gql.generated";
import { updateProductTagMutation } from "./ProductTagForm.gql";
import { GQLUpdateProductTagMutation } from "./ProductTagForm.gql.generated";
import { GQLUpdateProductTagMutationVariables } from "./ProductTagForm.gql.generated";
import isEqual from "lodash.isequal";
type FormValues = GQLProductTagFormFragment;
interface FormProps {
    onCreate?: (id: string) => void;
    id?: string;
}
export function ProductTagForm({ onCreate, id }: FormProps) {
    const client = useApolloClient();
    const mode = id ? "edit" : "add";
    const formApiRef = useFormApiRef<FormValues>();
    const stackSwitchApi = useStackSwitchApi();
    const { data, error, loading, refetch } = useQuery<GQLProductTagQuery, GQLProductTagQueryVariables>(
        productTagQuery,
        id ? { variables: { id } } : { skip: true },
    );
    const initialValues = useMemo<Partial<FormValues>>(
        () =>
            data?.productTag
                ? {
                      ...filterByFragment<GQLProductTagFormFragment>(productTagFormFragment, data.productTag),
                  }
                : {},
        [data],
    );
    const saveConflict = useFormSaveConflict({
        checkConflict: async () => {
            const updatedAt = await queryUpdatedAt(client, "productTag", id);
            return resolveHasSaveConflict(data?.productTag.updatedAt, updatedAt);
        },
        formApiRef,
        loadLatestVersion: async () => {
            await refetch();
        },
    });
    const handleSubmit = async (formValues: FormValues, form: FormApi<FormValues>, event: FinalFormSubmitEvent) => {
        if (await saveConflict.checkForConflicts()) throw new Error("Conflicts detected");
        const output = formValues;
        if (mode === "edit") {
            if (!id) throw new Error();
            const { ...updateInput } = output;
            await client.mutate<GQLUpdateProductTagMutation, GQLUpdateProductTagMutationVariables>({
                mutation: updateProductTagMutation,
                variables: { id, input: updateInput },
            });
        } else {
            const { data: mutationResponse } = await client.mutate<GQLCreateProductTagMutation, GQLCreateProductTagMutationVariables>({
                mutation: createProductTagMutation,
                variables: {
                    input: output,
                },
            });
            const id = mutationResponse?.createProductTag.id;
            if (id) {
                setTimeout(() => {
                    onCreate?.(id);
                    if (!event.navigatingBack) {
                        stackSwitchApi.activatePage(`edit`, id);
                    }
                });
            }
        }
    };
    if (error) throw error;
    if (loading) {
        return <Loading behavior="fillPageHeight" />;
    }
    return (
        <FinalForm<FormValues>
            apiRef={formApiRef}
            onSubmit={handleSubmit}
            mode={mode}
            initialValues={initialValues}
            initialValuesEqual={isEqual} //required to compare block data correctly
            subscription={{}}
        >
            {() => (
                <>
                    {saveConflict.dialogs}
                    <>
                        <TextField
                            required
                            variant="horizontal"
                            fullWidth
                            name="title"
                            label={<FormattedMessage id="productTag.title" defaultMessage="Title" />}
                        />
                    </>
                </>
            )}
        </FinalForm>
    );
}
