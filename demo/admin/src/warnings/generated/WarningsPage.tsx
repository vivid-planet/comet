// This file has been generated by comet admin-generator.
// You may choose to use this file as scaffold by moving this file out of generated folder and removing this comment.

import { Stack, StackPage, StackSwitch, StackToolbar } from "@comet/admin";
import { ContentScopeIndicator } from "@comet/cms-admin";
import * as React from "react";
import { useIntl } from "react-intl";

import { WarningForm } from "./WarningForm";
import { WarningsGrid } from "./WarningsGrid";

export function WarningsPage(): React.ReactElement {
    const intl = useIntl();
    return (
        <Stack topLevelTitle={intl.formatMessage({ id: "warnings.warnings", defaultMessage: "Warnings" })}>
            <StackSwitch>
                <StackPage name="grid">
                    <StackToolbar scopeIndicator={<ContentScopeIndicator global />} />
                    <WarningsGrid />
                </StackPage>
                <StackPage name="edit" title={intl.formatMessage({ id: "warnings.editWarning", defaultMessage: "Edit Warning" })}>
                    {(selectedId) => <WarningForm id={selectedId} />}
                </StackPage>
                <StackPage name="add" title={intl.formatMessage({ id: "warnings.addWarning", defaultMessage: "Add Warning" })}>
                    <WarningForm />
                </StackPage>
            </StackSwitch>
        </Stack>
    );
}
