// This file has been generated by comet api-generator.
// You may choose to use this file as scaffold by moving this file out of generated folder and removing this comment.
import { filtersToMikroOrmQuery, searchToMikroOrmQuery } from "@comet/cms-api";
import { ObjectQuery } from "@mikro-orm/core";
import { Injectable } from "@nestjs/common";

import { ShopProductVariant } from "../entities/shop-product-variant.entity";
import { ShopProductVariantFilter } from "./dto/shop-product-variant.filter";

@Injectable()
export class ShopProductVariantsService {
    getFindCondition(options: { search?: string; filter?: ShopProductVariantFilter }): ObjectQuery<ShopProductVariant> {
        const andFilters = [];

        if (options.search) {
            andFilters.push(searchToMikroOrmQuery(options.search, ["product.name", "product.description", "product.category", "size", "color"]));
        }

        if (options.filter) {
            andFilters.push(filtersToMikroOrmQuery(options.filter));
        }

        return andFilters.length > 0 ? { $and: andFilters } : {};
    }
}
