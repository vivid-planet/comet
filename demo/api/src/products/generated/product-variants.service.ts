// This file has been generated by comet api-generator.
// You may choose to use this file as scaffold by moving this file out of generated folder and removing this comment.
import { filtersToMikroOrmQuery, searchToMikroOrmQuery } from "@comet/cms-api";
import { ObjectQuery } from "@mikro-orm/core";
import { Injectable } from "@nestjs/common";

import { ProductVariant } from "../entities/product-variant.entity";
import { ProductVariantFilter } from "./dto/product-variant.filter";

@Injectable()
export class ProductVariantsService {
    getFindCondition(options: { search?: string; filter?: ProductVariantFilter }): ObjectQuery<ProductVariant> {
        const andFilters = [];

        if (options.search) {
            andFilters.push(
                searchToMikroOrmQuery(options.search, [
                    "name",
                    "product.title",
                    "product.status",
                    "product.slug",
                    "product.description",
                    "product.type",
                ]),
            );
        }

        if (options.filter) {
            andFilters.push(filtersToMikroOrmQuery(options.filter));
        }

        return andFilters.length > 0 ? { $and: andFilters } : {};
    }
}
